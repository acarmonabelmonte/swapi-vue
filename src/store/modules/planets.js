// Planets module
import axios from 'axios';

const uri = `${process.env.VUE_API_URL}planets/`;

const state = {
    planets: []
};

const getters = {
};

const actions = {
};

const mutations = {
};

export default {
    state, getters, actions, mutations
}
