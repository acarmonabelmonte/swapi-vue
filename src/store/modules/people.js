// People module
import axios from 'axios';

const uri = `${process.env.VUE_API_URL}people/`;

const state = {
    people: []
};

const getters = {
};

const actions = {
};

const mutations = {
};

export default {
    state, getters, actions, mutations
}
