// Starships module
import axios from 'axios';

const uri = `${process.env.VUE_API_URL}starships/`;

const state = {
    starships: []
};

const getters = {
};

const actions = {
};

const mutations = {
};

export default {
    state, getters, actions, mutations
}
